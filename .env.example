# Optional: a normal `python3 main.py` run uses saved evidence, with no API keys.
# Copy this file to .env only if you choose to enable new paid requests.
openrouter_api_key=

# Historical Voyage pilot only; not required by the current invoice workflow.
voyage_api_key=
